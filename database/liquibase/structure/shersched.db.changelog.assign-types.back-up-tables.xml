<?xml version="1.1" encoding="UTF-8" standalone="no"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog" xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.5.xsd">
    <!-- Code tables had not been completely implemented in the application prior at the point when I started work on code tables and management. Drop and re-create any relevant code table related tables so we can start from a clean state. -->
    <!-- Changesets -->
    <!-- NOTE: We may have to create a function to truncate if the table exists... execute the following script manually if there are any issues (uncomment the lines first) -->
    <changeSet author="Lucas Lopatka" id="truncate-code-tables">
      <!-- ONLY truncate tables if in DEV or TEST -->
      <sql>
        -- TRUNCATE TABLE shersched."assignment" CONTINUE IDENTITY CASCADE;
        -- TRUNCATE TABLE shersched.courtroom CONTINUE IDENTITY CASCADE;
        -- TRUNCATE TABLE shersched.court_role_code CONTINUE IDENTITY CASCADE;
        -- TRUNCATE TABLE shersched.jail_role_code CONTINUE IDENTITY CASCADE;
        -- TRUNCATE TABLE shersched.other_assign_code CONTINUE IDENTITY CASCADE;
      </sql>
    </changeSet>
    <!-- TODO: When implementing prod migration we may need to copy the assignment table because we will need its data -->
    <changeSet author="Lucas Lopatka" id="reset-code-tables">
      <sql>
      </sql>
    </changeSet>
</databaseChangeLog>
