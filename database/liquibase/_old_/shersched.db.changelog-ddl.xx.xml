<?xml version="1.1" encoding="UTF-8" standalone="no"?>
<databaseChangeLog 
	  xmlns="http://www.liquibase.org/xml/ns/dbchangelog" 
	  xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext" 
	  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	  xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.5.xsd">

    
    <!--
        catalogName="${POSTGRES_CATALOG}" schemaName="${POSTGRES_SCHEMA}" 
        baseTableCatalogName="${POSTGRES_CATALOG}" baseTableSchemaName="${POSTGRES_SCHEMA}" referencedTableCatalogName="${POSTGRES_CATALOG}" referencedTableSchemaName="${POSTGRES_SCHEMA}" 
    -->

    <changeSet author="Carol Geisler" id="tag0">
    	<tagDatabase tag="ddl_set_09_justin_ids_start"/>
    </changeSet>

    <!-- Comment for changeset group -->

    <!-- Changsets -->
    <changeSet author="Carol Geisler" id="ADDCOL_location_justin_ids">
    	<comment>Add the missed audit columns to the Leave table</comment>
    	  <addColumn catalogName="${POSTGRES_CATALOG}" schemaName="${POSTGRES_SCHEMA}" tableName="courthouse">
            <column name="justin_id" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>
            <column name="justin_code" type="VARCHAR(10)">
                <constraints nullable="false"/>
            </column>
	    	</addColumn>
    </changeSet>

    <changeSet author="Carol Geisler" id="tag1">
    	<tagDatabase tag="ddl_set_09_justin_ids_end"/>
    </changeSet>

</databaseChangeLog>
